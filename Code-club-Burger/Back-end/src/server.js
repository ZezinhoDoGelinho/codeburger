import app from "./app"

app.listen(3001)

//o método listen é usado para iniciar o servidor na porta especificada,
//Isso significa que seu aplicativo estará ouvindo por requisições HTTP na porta 3001 do servidor onde está sendo executado